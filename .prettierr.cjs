const config = {
  trailingComma: "es5",
  tabWidth: 4, //dk why it is still 2
  printWidth: 100, // length to which formatter would wrap on
  semi: false,
  singleQuote: true,
};

export default config;
